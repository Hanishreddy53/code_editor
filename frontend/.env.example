# Frontend environment variables for Vite (Vercel)
# Set VITE_BACKEND_URL to the deployed backend base URL (no trailing /api)
# Example: https://code-editor-backend.onrender.com
VITE_BACKEND_URL=

# Optional: WebSocket URL. If not provided, WS_URL will be derived from VITE_BACKEND_URL
VITE_WS_URL=
